import React from 'react';
import ReactDOM from 'react-dom/client';
import { SynthiaApp } from './SynthiaApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SynthiaApp />
  </React.StrictMode>
);
